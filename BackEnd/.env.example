# Jaay-Baan Backend Environment Configuration
# =============================================

# Database Configuration
DB_NAME=jaaybaan_db
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=db
DB_PORT=5432

# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=*

# Superuser Settings (for first-time setup)
# ==========================================
# These credentials will be used to create the initial admin user automatically
# when the application starts for the first time. After the first login, 
# it's recommended to change the password for security.
# 
# The superuser creation happens during container startup if no user with 
# the specified username exists in the database.
SUPERUSER_USERNAME=admin
SUPERUSER_EMAIL=admin@jaay-baan.local
SUPERUSER_PASSWORD=change-this-password

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Media Settings
MEDIA_URL=/media/
STATIC_URL=/static/
